# Version: 2.0.0

runtime:
  image: "python:3.11"

commands:
  package: ""
  build:
    - "pip install -r requirements.txt"
  start: "python3 app.py"

repository:
  branch: "main"
  auto_deploy: true
