runtime:
  image: "python:3.10-alpine"

commands:
  package: ""
  build:
    - "pip install --no-cache-dir -r requirements.txt"
  start: "python3 app.py"

repository:
  branch: "main"
  auto_deploy: true
